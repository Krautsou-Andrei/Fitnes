export const enum AchievementsKeyConfig {
    ONE_WEEK = 'one-week',
    ONE_MONTH = 'one-month',
    ALL_TIME = 'all-time',
}

export const enum AchievementsDefaultConfig {
    NUMBERS_DAYS_WEEK = 7,
    NUMBERS_DAYS_MONTH = 28,
}
