import { ReactElement } from 'react';

export type ResultCard = {
    buttonBlock: boolean;
    buttonTitle: string;
    description: string;
    icon: ReactElement;
    title: string;
    testId: string;
};
